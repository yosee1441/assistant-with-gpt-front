import React from 'react'
import ReactDOM from 'react-dom/client'
import { ReactGptApp } from './app'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ReactGptApp />
  </React.StrictMode>,
)

